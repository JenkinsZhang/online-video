server:
  port: 9092
  servlet:
    context-path: /business

spring:
  application:
    name: business
  redis:
    host: 34.105.212.23
eureka:
  client:
#    fetch-registry: false
    service-url:
      defaultZone: http://discovery:9090/eureka
  instance:
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 15


